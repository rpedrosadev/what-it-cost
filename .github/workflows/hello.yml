name: hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a greeting
        run: |
          echo "first CICD steps!"
      - name: Obtener fecha
        run: date
      - run: ls -l
